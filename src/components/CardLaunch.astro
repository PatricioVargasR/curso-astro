---
interface Props {
    id: string,
    name: string,
    img: string,
    success: boolean,
    flightNumber: number,
    details: string
}

const { success, flightNumber, img, id, details } = Astro.props
const succesText = success ? "Éxito" : "Fracaso"

---

<!--TODO: Terminar CardLaunch - tiempo: 1:00:00 -->